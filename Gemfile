source "https://rubygems.org"

# lock jekyll to the 4.x line (5.0 is still in flux rn)
gem "jekyll", "~> 4.4.1"

# pin sass-embedded bc jekyll-sass-converter >=3 requires it
gem "sass-embedded", "~> 1.92"

# ensure listen works nicely on linux
gem "listen", "~> 3.9"

# optional: fix windows compat (safe to leave if not on windows)
gem "webrick", "~> 1.9"
